import styled from "styled-components";

export default function Home() {
  return <HeaderWrapper>Private Round</HeaderWrapper>;
}

const HeaderWrapper = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem;
`;
